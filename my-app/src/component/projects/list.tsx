import React from "react";


function projectList(props:any) {
/*     return (
        <>
            <div>
                <ul>
                {props.list.map(entry => {
                    return (
                        <projectListItem key={ entry.id } line={ entry } />
                    );
                })}
                </ul>
            
            </div>

        </>

    ) */
    

}